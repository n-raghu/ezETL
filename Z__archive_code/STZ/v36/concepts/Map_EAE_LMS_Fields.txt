gist:
Map EAE Fields with LMS fields using a SQL Table
Now parse and create sql query in python
Split 'eae_obj' field if having '.' to configure as array.

conclusion:
Unable to identify the join conditions in case of mulitple joins using multiple conditions.

code:
GO
IF OBJECT_ID('AAA',N'U') IS NOT NULL
DROP TABLE AAA
GO
CREATE TABLE AAA(
mapID INT IDENTITY(1,1),
isActive BIT NOT NULL,
isClause BIT NOT NULL,
isColumn BIT NOT NULL,
isJoin BIT NOT NULL,
appVersion VARCHAR(16),
parent_obj VARCHAR(128),
child_obj VARCHAR(128),
eae_obj VARCHAR(128),
)
GO
INSERT AAA
	SELECT 1,1,1,1,5.16,'TBL_CUSTOMER_LOOKUP','FLDCUSTID','orgid'
	UNION ALL
	SELECT 1,1,1,1,5.16,'TBL_CUSTOMER_LOOKUP','FLDCUSTOMERNAME','orgName'
	UNION ALL
	SELECT 1,1,1,1,5.16,'TBL_STUDENT_LOOKUP','FLDCUSTOMERNAME','orgName'
GO

SELECT * FROM AAA
