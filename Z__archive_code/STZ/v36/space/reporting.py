class rData():
	def __init__(mem):
		mem.a='new'

	def ctStats(mem):
		print('Guide to create customer Stattistics')
		print('ll=[(int(dat["custid"][0]),len(dat)) for dat in dataTower if len(dat)>0]')
		print("bar_chart=pygal.Bar(style=LightStyle,width=800,height=600,legend_at_bottom=True, human_readable=True,title='Customers and Active Learners migrated to StatZen')")
		print("for index,row in cst.iterrows(): bar_chart.add(row['cus'],row['total']) ")
		
		return 'X'

class repos():
	def __init__(mem):
		mem.a='new'

	def ctStats(mem):
		return 'X'